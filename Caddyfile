# Домен берётся из .env рядом с docker-compose.yml
{$DOMAIN} {
	reverse_proxy coffee:3000
	encode zstd gzip
}

# www уводим на голый домен, чтобы не плодить две версии сайта
www.{$DOMAIN} {
	redir https://{$DOMAIN}{uri} permanent
}
